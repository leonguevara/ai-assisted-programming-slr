Vamos a intentar realizar esta tarea otra vez.  En el archivo ZIP (Possible Primaries v18.zip), vienen los archivos (en subfolders) que me has dicho que podríamos integrar a nuestro corpus, así como un archivo .bib y un archivo .csv con la info de los papers.  Me gustaría que agregaras sus datos al archivo de Excel que también te anexo (SLR_Evidence_Master_Book_for_Categorisation.xlsx), en la hoja SLR Evidence.  De esta hoja, puedes llenar la columna Paper_ID con los consecutivos que tocan: el último que usamos fue el Paper_2979, así que seguiríamos con el 300, 301, y así. Las columnas Referencia y Name se explican solas.  Recuerda no juntarlas; como decía mi abuelita: "un lugar para cada cosa y cada cosa en su lugar".  La columna In SLR siempre sería Yes; ya después descartaremos. En la columna Rol, casi todos son "Evidencia empírica", aunque luego comenzaste a llenar con datos más detallados; de todas formas, todos son prácticamente empíricos y por eso los queremos agregar.  En la columna Study_type, también todos deberían ser Primary.  Los secundarios los estamos enviando como related work; entiendo que podríamos considerar secundarios algunos papers que hemos clasificado como nivel B, pero por lo pronto son primarios porque forman parte del corpus (por el momento). Hay que llenar las columnas de Claims_soporta, Claims_no_soporta (en caso de ser relevante porque hay celdas vacías en el original), Tipo_Evidencia, Dataset_contexto (en caso de ser relevante porque hay celdas vacías en el original), Métricas, Herramientas, Hallazgos_clave, Nivel_Soporte, Observaciones, Notas_TEX y  Clasificación (A1, A2 o B). Nos interesa llenar las columnas de Primary_Dimension_Normalized, DIM_PROD, DIM_CQTD, DIM_HAI, DIM_PT, DIM_ECON, DIM_ORG, DIM_CLHF y Dimension_Code_notes.  Como salida, espero el archivo Excel modificado, ya con los datos de los nuevos archivos incluidos. Revisa bien los archivos incluidos en el ZIP, no importa que tardes más.



Vamos a intentar realizar lo mismo pero con Related Work.  En el archivo ZIP (Related Work v4.zip), vienen los archivos (en subfolders) que me has dicho que podríamos integrar a nuestro estudio como Related Work, así como un archivo .bib y un archivo .csv con la info de los papers.  Me gustaría que agregaras sus datos al archivo de Excel que también te anexo (SLR_RelatedWork_Master_Book_for_Categorisation.xlsx), en la hoja SLR Evidence.  De esta hoja, puedes llenar la columna Paper_ID con los consecutivos que tocan: el último que usamos fue el Related_064, así que seguiríamos con el 065, 066, y así. Las columnas Referencia y Name se explican solas.  Recuerda no juntarlas; como decía mi abuelita: "un lugar para cada cosa y cada cosa en su lugar".  La columna In SLR siempre sería Yes; ya después descartaremos. En la columna Rol, todos son "Related Work / evidencia secundaria".  En la columna Study_type, también todos deberían ser Related Work.  Hay que llenar las columnas de Claims_soporta, Claims_no_soporta (en caso de ser relevante porque hay celdas vacías en el original), Tipo_Evidencia, Dataset_contexto (en caso de ser relevante porque hay celdas vacías en el original), Métricas, Herramientas, Hallazgos_clave, Nivel_Soporte, Observaciones, Notas_TEX y  Clasificación (Le has puesto Related Work a todos los anteriores, así que supongo aquí también). Nos interesa llenar las columnas de Primary_Dimension_Normalized, DIM_PROD, DIM_CQTD, DIM_HAI, DIM_PT, DIM_ECON, DIM_ORG, DIM_CLHF y Dimension_Code_notes.  Como salida, espero el archivo Excel modificado, ya con los datos de los nuevos archivos incluidos. Revisa bien los archivos incluidos en el ZIP, no importa que tardes más.


